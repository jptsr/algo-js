// Write a program that will display the minimum and maximum elements of a given array.
// array of numbers I guess (possible w/strings ?)

let arr = [3, 1, 4, 5, 2];
let min = Math.min(...arr);
let max = Math.max(...arr);
console.log(min, max);